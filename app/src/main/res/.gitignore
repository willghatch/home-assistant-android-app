# Fork-local: generated by set-real-hass-cert.sh, which the build requires. Kept in
# this new file rather than the root .gitignore so the fork never conflicts with upstream.
# The resource merger skips dotfiles, so this file does not become a resource.
/raw/homelab_ca.pem
/xml/network_security_config.xml
